import React from 'react';
import AvatarImage from '../../src/components/atoms/AvatarImage';

const MyPages = (user, handleLogout) => {
  return <div>{/* <AvatarImage user={user} /> */}</div>;
};

export default MyPages();
